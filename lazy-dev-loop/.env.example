# Required: LLM API key for OpenCode
OPENAI_API_KEY=sk-...

# Optional overrides
BRIDGE_PORT=7777
RUN_TIMEOUT=120
DEBUG_OUTPUT=true
TMUX_SESSION_NAME=lazy-dev-loop

# UID/GID matching your host user (default: 1000:1000)
# Run `id -u` and `id -g` on the host to find yours
UID=1000
GID=1000
