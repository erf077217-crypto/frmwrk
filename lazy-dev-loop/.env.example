# Required: LLM API key for OpenCode
OPENAI_API_KEY=sk-...

# Optional overrides
BRIDGE_PORT=7777
RUN_TIMEOUT=120
DEBUG_OUTPUT=true
TMUX_SESSION_NAME=lazy-dev-loop

# (No longer needed — entrypoint drops privileges to app user automatically)
