# Agents

Agent runtime and definitions.

Every agent shares a common lifecycle (init → run → complete → cleanup)
and can be assigned tools and an LLM provider.

- `base.py` — Abstract BaseAgent
- `coding_agent.py` — Skeleton
- `documentation_agent.py` — Skeleton
- `testing_agent.py` — Skeleton
